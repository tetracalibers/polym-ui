import {
  normalizedPath,
  getDirRelativeDepth,
  returnDir,
  doAfterMoveDir,
  createFile,
} from './module/shelljsWrapper'

export {
  normalizedPath,
  getDirRelativeDepth,
  returnDir,
  doAfterMoveDir,
  createFile,
}
